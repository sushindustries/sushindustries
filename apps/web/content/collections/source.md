---
title: The source
summary: Every component and package file, heaviest first - what the library is made of.
kind: source
sort: tokens
direction: desc
limit: 100
draft: false
---

The `.tsx`, `.ts` and `.css` files inside `packages/*/src`. Not documentation
about the code - the code, indexed so it can be searched and quoted without a
checkout.

Sorted by weight, which makes this the answer to a question no directory
listing gives you: what is the most expensive thing here to read, and is it
expensive because it is doing something hard or because it grew.
