{
	"name": "@sushindustries/sync",
	"version": "0.1.0",
	"description": "Live Postgres rows over Electric: the proxy that defines a shape on the server, and the collection that reads it on the client.",
	"type": "module",
	"license": "MIT",
	"publishConfig": {
		"access": "public"
	},
	"exports": {
		".": "./src/index.ts",
		"./proxy.server": "./src/proxy.server.ts",
		"./package.json": "./package.json"
	},
	"files": [
		"src"
	],
	"dependencies": {
		"@electric-sql/client": "^1.5.26",
		"@tanstack/electric-db-collection": "^0.4.0",
		"@tanstack/react-db": "^0.3.0"
	}
}
