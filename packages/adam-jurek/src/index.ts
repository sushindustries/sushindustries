/*
 * The umbrella. One import surface for the whole component library - this
 * package is how "install the system" stays one sentence. It adds nothing:
 * anything that would earn a line of its own here belongs in `ui`, and the
 * stylesheet stays `@sushindustries/atoms` because CSS is not re-exportable.
 */
export * from "@sushindustries/ui";
