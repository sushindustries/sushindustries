<!-- template
target: packages/ui/docs/{slug}/api.md
tokens: slug, title
-->
---
title: {title} API
summary: Every prop, what it defaults to, and what happens when it is wrong.
---

## Props

| Prop | Type | Default | Does |
| --- | --- | --- | --- |
| `children` | `ReactNode` | - | |

## Notes

Anything a type signature cannot say: which combinations are meaningless, which
prop is ignored when another is set, and what the component does when it is
handed something it cannot render.
